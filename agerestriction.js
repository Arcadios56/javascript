const prompt = require('prompt-sync')();

function ageRestriction
	let minimumAge = 10;
let age = parseInt(prompt("Enter a age: "));
if (age >= minimumAge) {
console.log("Welcome to the show!")

} else {
	console.log("Sorry, you're too young!")
}
}

concole.log(ageRestriction())